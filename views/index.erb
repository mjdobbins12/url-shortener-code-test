<h2>Enter the url you'd like to shorten:</h2>
<form action='/url' method='post'>
<input type='text' name='url'>
<input type='submit' value='submit'>
</form>

<p>Your previously created links will appear below. Add the 8-letter mini-URL to 'localhost:8080/' to open a link in a new tab</p>

<ul>
  <% Shortener.display_urls.each do |short_url, long_url| %>
    <li> <%= short_url %>: <a href=<%= long_url %>><%= long_url %></a></li>
  <% end %>
</ul>